---
description: Start the Vite development server for local testing
---

# Start Development Server

// turbo

1. Run the development server:

```bash
npm run dev
```

2. The app will be available at http://localhost:5173

3. Open `live.html` for the Live Transfer script or `preclosed.html` for the Preclosed script
