---
description: Deploy the application to Git (push changes)
---

# Deploy to Git

1. Stage all changes:

```bash
git add .
```

2. Commit with a message:

```bash
git commit -m "Update: describe your changes"
```

// turbo 3. Push to the remote repository:

```bash
git push
```
